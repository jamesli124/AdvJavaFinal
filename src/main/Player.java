package main;

import javafx.scene.image.Image;

public class Player extends Sprite{

    public Player(double x, double y, double h, double w, Image img)
    {
        super(x, y, h, w, img);
    }
}
